BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \   
    ${LAYERDIR}/recipes-*/*/*.bbappend"
BBFILE_COLLECTIONS += "ecepi"
BBFILE_PATTERN_mtdock = "^${LAYERDIR}/"
BBFILE_PRIORITY_mtdock = "50"
